# 작업 맥락 노트

이 파일은 다음 작업자(사람이든 AI든)가 결정의 배경을 다시 추론하지 않도록 남기는 기록이다.

## 2026-08-20 개선 작업

### 사용자가 정한 방향
- **사진** — 선생님이 다른 프로그램에서 따로 작업하기로 함. 이번 작업에서는 새 사진을 넣지 않고, **다시 밀리지 않는 구조로만** 바꾼다.
- **문제 수** — 나라당 6문제, 특수칸(기후/지형) 각 18문제.
- **통행세** — "게임의 재미를 위해 파산은 있어야 한다." 즉 통행세를 낮춰 파산을 없애는 방향이 아니라, **파산을 정상 규칙으로 살리되 게임이 멈추지 않게** 고친다.

### 사진 구조를 왜 바꾸는가
기존 `landmarkPhotos`는 `spaces`와 인덱스로만 연결된 별도 배열이었다. 특수칸 4개를 배열에 넣지 않아 전체가 한 칸씩 밀렸고, 배열 길이도 31개라 32번째 칸은 아예 값이 없었다.
→ 사진 키를 `spaces` 각 항목 안으로 옮긴다. 나라 데이터와 사진이 같은 객체에 있으면 칸을 추가·삭제해도 절대 어긋나지 않는다.
→ 선생님이 나중에 사진을 교체할 때도 나라 이름 바로 옆의 `photo` 값만 고치면 된다.

### 파산을 "탈락"으로 처리한 이유
기존 코드는 `checkGameOver('bankrupt')`를 호출했지만 그 함수는 라운드 초과와 시간 종료만 처리해서 항상 `false`를 반환했고, `endTurn()`도 부르지 않아 게임이 완전히 멈췄다.
선택지는 두 가지였다.
1. 파산 즉시 게임 전체 종료 → 4인 수업에서 한 명 때문에 전체가 끝나므로 부적절
2. 파산자만 탈락시키고 나머지는 계속 → 채택

파산자의 토지는 채권자에게 넘긴다(모노폴리 규칙). 생존자가 1명이 되면 그 시점에 게임을 끝낸다.
AI도 같은 규칙을 적용한다. 기존에는 AI만 "가진 만큼 부분 납부"라 사람과 규칙이 달랐다.

### 통행세 계산식을 조정한 근거
기존: `땅값 × (건물수+1) × 0.5` → 그리스(₩60,000) 건물 3단계 = ₩120,000. 시작 자금 ₩350,000의 34%가 한 번에 나감.
새 계산식: 건물 단계별 배율을 `[0.35, 0.7, 1.2, 1.9]`로 둔다.
- 건물 0단계 = 땅값의 35% (기존 50%보다 낮아 초반 부담 완화)
- 건물 3단계 = 땅값의 190% (기존 200%와 비슷하게 유지 → 파산 위험은 살아 있음)
의도: 초반에는 잘 안 망하고, 후반에 건물을 올린 땅이 진짜 무섭다. 파산은 "운 나빠서"가 아니라 "상대가 잘 키운 땅을 밟아서" 일어나게 만든다.

### 문제 중복 방지 방식
`usedQuizKeys` Set에 `"나라명#인덱스"`를 넣어 추적한다. 출제할 때 아직 안 쓴 것만 후보로 두고, 후보가 비면 그 나라 문제만 초기화한다.
전역 초기화가 아니라 풀 단위 초기화인 이유 — 특수칸 18문제가 다 소진돼도 나라 문제까지 같이 리셋되면 "방금 푼 문제"가 다시 나올 수 있다.

### 문제를 별도 파일로 분리한 이유
`questions.js` 204문제는 게임 로직과 성격이 완전히 다르다. 선생님이 학년·단원에 맞춰 문제만 손볼 때 게임 코드를 건드리지 않아도 되게 분리했다.
전역 `const COUNTRY_QUIZZES`, `CLIMATE_QUIZZES`, `LANDFORM_QUIZZES`로 노출하고 `index.html`에서 `script.js`보다 먼저 읽는다. 번들러 없이 그냥 script 태그 순서로 해결한다.

### 보너스 카드 설계
생태 쉼터를 단순 현금 지급에서 카드 뽑기로 바꿨다. 카드 8종.
- 즉시 효과형 5종 (출발지 이동, 원하는 땅으로 이동, 장학금, 한 칸 더, 기부)
- 보유형 3종 (통행세 면제권, 무료 증축권, 반값 매입권)
보유형은 `player.items` 배열에 쌓이고 해당 상황에서 자동 또는 선택 사용된다.
AI는 보유형 카드를 조건이 맞으면 무조건 사용한다(판단 로직을 단순하게 유지).

### 레이아웃 — 사이드바를 여백 가운데로
기존 `grid-template-columns: minmax(auto, 1fr) 390px`는 사이드바를 화면 오른쪽 끝에 붙였다.
새 방식: `grid-template-columns: min(97vh, 960px, 55vw) 1fr`로 첫 열을 보드 크기에 딱 맞추고, 둘째 열 안에서 `.sidebar { align-items: center }`로 카드를 가운데 정렬한다.
HTML 구조를 바꾸지 않고 CSS만으로 해결했다.

### 타일 태그 잘림
15개 칸에서 태그가 잘렸다. 원인은 두 가지가 겹쳤다.
1. 폰트가 타일 폭 대비 큼 → `clamp` 하한을 낮추고 `letter-spacing`을 더 좁힘
2. 태그 문구 자체가 김 → `온대계절풍`처럼 5글자가 한계. `이구아수/아마존`(8글자) 같은 건 문구를 줄였다.
기후 용어는 학습 핵심이라 줄이지 않고 폰트로 해결했고, 그 외 설명성 태그만 단축했다.

### 남은 과제
- 사진 파일 교체 (선생님 작업)
- 문제 난이도 검토 — 6학년 수준에 맞는지 실제 수업에서 확인 필요
- 접근성(키보드 조작, 스크린리더)은 손대지 않았다

### 작업 완료 후 추가 기록 (2026-08-20)

**토스트 위치를 두 번 옮겼다.**
처음에는 화면 하단 가운데(`position: fixed`)에 두었는데, 스크린샷으로 확인해 보니 보드 아래쪽 줄의 칸 이름(생태 쉼터·네팔·모로코 등)을 정확히 가렸다.
→ `.board-center` 안으로 옮겨 보드판 한가운데 빈 공간에 뜨게 했다. 이 자리는 원래 비어 있고 시선이 머무는 곳이라 알림 위치로 적합하다. 타일과 겹침 0건 확인.

**제작자 크레딧을 왼쪽 아래로 옮겼다.**
사이드바가 오른쪽 여백 가운데로 오면서 `right: 20px; bottom: 12px`에 고정된 크레딧과 겹쳤다. 왼쪽 아래로 옮겨 해결.

**보너스 카드 보유 칸은 비어 있으면 숨긴다.**
4인 플레이에서 카드 4장이 세로로 길어져 마지막 카드가 잘렸다. "보유 카드 없음" 줄을 항상 표시하지 않고 카드가 있을 때만 보이게 해서 높이를 확보했다.

**타일 이름 글자 크기 기준을 5글자로 낮췄다.**
원래는 6글자 이상만 `long-name`(작은 글씨)이었는데, `인도네시아`·`아이슬란드`(5글자)가 넘쳤다. 기준을 5글자로 내리고 clamp 상한도 낮췄다.

**검증 스크립트에서 겪은 함정 기록.**
테스트 코드에서 `window.pickQuiz = null`로 덮어썼더니 게임 전체가 `pickQuiz is not a function`으로 죽었다. 이 스크립트는 모듈이 아니라 전역 스코프에서 도는 일반 스크립트라, 테스트에서 전역 이름을 건드리면 게임이 그대로 망가진다. 앞으로 브라우저 자동 테스트를 붙일 때 전역을 덮어쓰지 말 것.

### 사진 작업 (2026-08-20, 크롬 연동)

**왜 처음에 사진을 안 건드렸는가.**
이 작업 환경(클라우드 컨테이너)은 `images.unsplash.com` 연결이 차단되어 있다. DNS는 풀리지만 TCP 연결이 안 된다. 그래서 사진 주소를 코드에 넣을 수는 있어도 그게 어느 나라 사진인지 확인할 방법이 없었다. 확인 없이 넣으면 기존 버그를 되풀이할 뿐이라 손대지 않았다.

**어떻게 해결했는가.**
선생님 PC의 크롬을 원격으로 빌려 썼다. 크롬은 학교 인터넷을 그대로 쓰므로 Unsplash에 접속된다.
1. `unsplash.com` 페이지 안에서 `fetch('/napi/search/photos?query=...')` 로 후보 목록을 받아온다 (같은 출처라 CORS 문제 없음)
2. 그 페이지의 DOM을 통째로 갈아끼워 **번호가 붙은 대조표**를 만든다 — 스크린샷 한 장으로 후보 12~16장을 눈으로 비교할 수 있고, 번호로 고르면 ID가 어긋날 일이 없다
3. 고른 사진 두 장을 784×523 크기로 나란히 띄우고 스크린샷을 파일로 저장
4. 컨테이너에서 좌우로 잘라 `images/<나라>.jpg` 로 저장

**왜 스크린샷으로 받았는가.**
`fetch`로 이미지를 base64로 받아 넘기는 방법도 되지만, 한 장에 2~3만 토큰이 들어 28장이면 감당이 안 된다. 스크린샷은 파일로 바로 저장되므로 훨씬 싸다. 화질 손실이 있지만 타일은 최대 105px, 정보 카드는 460px라 784px 원본으로 충분하다.

**겪은 문제.**
DOM을 갈아끼운 직후 첫 스크린샷이 거의 항상 CDP 타임아웃(30초)을 냈다. 두 번째 시도는 성공한다. 렌더러가 큰 이미지를 그리는 동안 캡처가 막히는 것으로 보인다. 재시도로 넘겼다.

**외부 URL에서 로컬 파일로 바꾼 이유.**
선생님이 "내려받아 폴더에 저장" 방식을 선택했다. 학교 망이 Unsplash를 막으면 검은 칸만 남는 위험이 사라지고, 로딩도 빨라진다. `photoUrl()` 함수가 이제 `space.photo`를 그대로 돌려주므로 나중에 다른 이미지 서비스로 바꾸려면 이 함수 하나만 고치면 된다.

**사진 28장의 실제 확인 결과.**
원본 목록은 밀림을 바로잡아도 13장이 다른 나라 사진이었다. 예를 들어 한국 칸은 중국 상하이, 필리핀은 두바이, 네팔은 마추픽추였다. 마추픽추는 페루 칸으로 옮겨 살렸다. 자세한 내역은 사진확인결과.md 참고.

### 국기와 정보 팝업 (2026-08-20)

**국기를 이미지 파일로 넣은 이유.**
처음에는 이모지 국기(🇰🇷)를 쓸까 했지만, **윈도우 크롬은 국기 이모지를 국기로 그리지 않고 "KR" 같은 글자로 표시한다.** 선생님 환경이 윈도우이므로 쓸 수 없다. 직접 SVG로 28개를 그리는 것도 영국·미국·브라질·멕시코처럼 복잡한 국기에서 정확도를 보장하기 어렵다.
→ flagcdn.com(퍼블릭 도메인)에서 받아 `images/flags/`에 넣었다. 국가 코드가 곧 파일 이름이라 대응 관계가 명확하다.

**국기를 자를 때 겪은 실수.**
사진 캡처 때와 달리 이 탭은 `window.innerWidth`가 1920인데 스크린샷은 1568px로 저장된다. `getBoundingClientRect()`가 주는 CSS 픽셀 좌표를 그대로 자르면 전부 어긋난다. 배율 `1568/innerWidth = 0.8167`을 곱해야 한다. 처음에 이걸 놓쳐서 모로코 국기가 통째로 배경색만 잘렸다.
→ 캡처 전에 항상 `1568/window.innerWidth`를 확인할 것.

**배경을 자홍색(#ff00ff)으로 둔 이유.**
잘라낸 뒤 남는 여백을 자동으로 제거하려면 배경색이 국기에 없는 색이어야 한다. 흰색으로 두면 일본 국기(흰 바탕에 붉은 원)가 붉은 원만 남게 잘린다. 어떤 국기에도 없는 자홍색을 쓰면 안전하게 테두리만 제거할 수 있다.
네팔 국기는 유일한 비직사각형 국기라 여백 제거로는 해결되지 않아, 자홍색 픽셀을 투명하게 바꿨다.

**정보 팝업을 2단으로 만든 이유.**
설명을 4~5문장으로 늘리고 글씨도 키우자 768px 높이 화면에서 32칸 중 20칸이 스크롤을 요구했다. 수업 중 스크롤은 번거롭다.
→ 사진과 설명을 좌우로 나눴다(46% : 나머지). 세로 공간을 절반으로 줄여 1280×720에서도 전부 한 화면에 들어간다. 사진이 없는 특수칸은 `.info-body.no-photo`로 1단이 된다.

**CSS 우선순위 함정.**
`.info-desc`에 글자 크기를 줬는데 적용되지 않았다. 기존 `.setup-card p { font-size: 14px }`가 (0,1,1)로 `.info-desc`(0,1,0)보다 우선하기 때문. `!important` 대신 `.setup-card p.info-desc`로 선택자를 강화해 해결했다.

**테스트 하네스의 오탐 기록.**
1인 AI 모드에서 "퀴즈 모달 교착"이 보고됐지만 실제 버그가 아니었다. AI가 퀴즈를 푸는 3.8초 동안 선택지 버튼이 의도적으로 잠겨 있는데, 하네스가 이를 "멈춤"으로 세고 카운터를 초기화하지 않아 한도를 넘겼다.
→ 판정 기준을 "내부 상태 스냅샷이 20회(약 14초) 연속 완전히 동일할 때"로 바꿔 6판을 돌렸고 전부 정상 종료했다. 자동 테스트에서 "버튼이 잠김"과 "게임이 멈춤"을 구분할 것.

### 보드 타일 배치 (2026-08-20)

**나라 이름이 사진을 덮는 문제.**
원래 `.tile-name`이 사진 위에 절대적으로 얹혀 있었고, 글자를 읽히게 하려고 `.tile.photo-tile::after`에 위 0.68 / 아래 0.88의 짙은 그라데이션을 깔아 놨다. 결과적으로 사진이 거의 검게 보였다.
→ 이름을 흐름 안으로 되돌려 타일 맨 아래의 불투명 띠(`rgba(23,26,22,.9)`)로 만들었다. 배경이 불투명하니 `text-shadow`도, 사진 위 어두운 막도 필요 없다. 그라데이션은 좌상단 태그 칩의 대비를 위해 위쪽 30%만 옅게 남겼다.

**배치 방식.**
`.tile`을 `flex-direction: column; justify-content: flex-end`로 두고 `order`로 쌓는다.
- `order: 0` 말 컨테이너 (`flex: 1`, `align-content: end`) — 남는 공간을 차지하고 말을 아래로 붙인다
- `order: 1` 소유자 배지
- `order: 2` 이름 띠
태그 칩과 특수칸 배지만 `position: absolute`로 좌상단·우상단에 띄운다.
절대 좌표(`bottom: 26%` 같은 값)를 쓰지 않았기 때문에 이름 띠 높이가 폰트에 따라 달라져도 말과 배지가 알아서 밀려난다.

**말 크기 버그.**
`.piece { width: min(24px, 30%) }`였는데, 부모 `.piece-slot`의 높이가 `auto`라 백분율 높이가 해결되지 않아 **말이 4px로 찌그러졌다.** 스크린샷에서는 숫자만 삐져나와 보여 처음엔 눈치채기 어려웠다.
→ `clamp(13px, 2.1vh, 24px)`로 화면 높이 기준으로 바꿨다. 보드가 `min(97vh, 960px)`이라 타일도 vh에 비례하므로 잘 맞는다.
교훈: 백분율 크기는 부모가 확정된 크기를 가질 때만 쓸 것.

**특수칸 이름 띠.**
특수칸은 배경이 이미 단색 그라데이션이라 이름 띠를 `rgba(0,0,0,.42)`로 반투명하게 두어 칸 색이 비치게 했다. 폰트도 `기후 퀴즈`(공백 포함 5글자)가 들어가도록 나라 칸과 같은 크기로 낮췄다.

### 보드판 가로형 전환 (2026-08-20)

**왜 9×9를 유지했는가.**
둘레 32칸을 유지하면서 가로로 넓히는 방법은 두 가지였다.
1. 격자를 10×8로 바꾸고 칸은 정사각 유지 → 네 모서리가 0·9·16·25번이 되어 **특수칸 간격이 8·8·8·8에서 9·7·9·7로 불균등**해진다. 퀴즈칸 조우 빈도가 달라져 게임 밸런스가 바뀐다.
2. 9×9 격자 유지, 칸만 가로로 넓힘 → 칸 순서·특수칸 위치·`route` 배열 전부 그대로. CSS만 수정.

효과가 거의 같고(타일 면적 기준 1366에서 +26%) 규칙을 건드리지 않는 2번을 택했다. 사용자도 이 안을 선택했다.

**칸 비율 상한을 1.5로 둔 이유.**
타일 사진이 3:2(1.5)다. 칸이 이보다 넓어지면 사진 위아래가 잘리기 시작한다. 1.5 이하로 두면 좌우만 살짝 잘리거나 딱 맞는다.

**높이 상한과 폭 계산을 함께 묶어야 한다.**
처음에 `height: min(97vh, 100%); width: min(100%, calc(min(97vh,100%) * 1.5)); max-height: 1100px` 로 썼더니, 2560×1440에서 높이는 `max-height`로 1100에 걸리는데 폭 계산은 여전히 97vh(1397)를 기준으로 해서 **칸 비율이 1.97까지 벌어졌다.**
→ `height: min(97vh, 1100px, 100%)` 로 상한을 안에 넣고, 폭 계산에도 **같은 식**을 쓰도록 고쳤다. 상한을 `max-height`로 따로 걸지 말 것.

**사이드바 열 폭.**
`grid-template-columns: 1fr minmax(400px, 440px)` 로 두면 여유가 있을 때 사이드바가 440까지 먹어 보드가 그만큼 줄었다. `minmax(0, 1fr) min(400px, 32vw)` 로 바꿔 사이드바를 고정하고 남는 폭을 전부 보드에 줬다. `32vw` 덕분에 좁은 화면에서는 사이드바가 먼저 줄어든다.

**세로 800px 이하 압축.**
칸이 커지며 사이드바가 400→392px가 되어 카드가 세로로 길어졌고 4인 플레이에서 넘쳤다. `@media (max-height: 800px)`로 카드 여백·간격을 줄여 1366×768에서 정확히 맞췄다.

### 가운데 지구본과 입체 주사위 (2026-08-20)

**대륙 윤곽을 손으로 그리지 않은 이유.**
지리 수업 자료다. 내가 좌표를 눈대중으로 찍으면 대륙 모양이 틀린다. Natural Earth 1:110m 해안선(퍼블릭 도메인)을 받아 Douglas-Peucker(0.45도)로 단순화하고 0.25도 격자로 반올림해 `worldmap.js`(18KB, 73고리 1650점)로 만들었다.
처음엔 사진·국기 때처럼 크롬을 빌리려 했는데, **컨테이너에서 raw.githubusercontent.com 이 직접 열렸다.** (github.com은 400이지만 raw는 200) 앞으로 공개 데이터는 컨테이너에서 먼저 시도해 볼 것.

**정사영 도법.**
`project(lat, lon)` 이 위경도를 화면 좌표로 바꾸고 `cosC >= 0` 으로 지구 뒤편을 걸러 낸다. 뒤편 점을 만나면 path의 펜을 떼서(`M`으로 다시 시작) 대륙이 가장자리를 가로질러 이어지지 않게 했다.

**주사위 면 매핑은 추측하지 말 것.**
"rotateX(90)이 윗면"이라는 기존 CSS 주석을 믿고 착지각을 손으로 계산했더니 6번이 틀렸다. 결국 브라우저에서 `DOMMatrix`로 모든 (x,y) 조합을 돌려 **어떤 각도에서 어떤 면이 카메라를 향하는지 실제로 측정**해 표를 확정했다.
확정된 표: 1={90,0}, 2={0,0}, 3={90,270}, 4={90,90}, 5={180,0}, 6={90,180}

**검증할 때 애니메이션이 끝났는지 확인할 것.**
처음 측정이 전부 틀리게 나왔는데, 원인은 `transition: 1s` 가 도는 중에 80ms만 기다리고 `getComputedStyle`을 읽었기 때문이었다. 중간 각도를 재고 있었다. 1.3초를 기다리니 6/6 일치했다.

**기울기는 각도 덧셈으로.**
`rotateX(A) rotateX(B) = rotateX(A+B)` 이므로, 고정 기울기 `DIE_TILT(-58도)`를 착지각에 그냥 더했다. 별도의 wrapper 요소가 필요 없다.

**가운데를 비운 이유.**
지구본은 도착한 나라가 **정중앙**에 오도록 회전한다. 그런데 제목과 차례 표시가 정중앙에 있어서 마커가 글자에 완전히 가려졌다. `.center-stack`을 `justify-content: space-between`으로 바꾸고 위/가운데(빈칸)/아래 3단으로 나눠 해결했다.

**성능.**
회전 중 매 프레임 SVG 문자열을 새로 만든다. 걱정했지만 한 프레임 1.4ms, 62fps로 여유로웠다. 대륙 점을 1650개로 줄여 둔 덕이다. 더 정밀한 데이터를 쓰려면 성능을 다시 재 볼 것.

### 턴 진행 순서와 나침반 제거 (2026-08-21)

**문제.**
`movePlayerStepByStep`이 끝나자마자 `updateGlobeForSpace()`와 `resolveLanding()`을 연달아 불렀다. 지구본은 0.9초에 걸쳐 도는데 퀴즈 모달이 즉시 화면을 덮어서, 애써 만든 지구본을 아무도 볼 수 없었다.

**해결.**
이동 완료 → 지구본 표시 → **대기** → 착륙 처리(퀴즈) 순으로 바꿨다. `movePlayerStepByStep`이 async 함수라 `await`로 간단히 끼워 넣었다.
대기 시간은 `globePauseFor(space)`가 정한다. `space.lat`이 있는 나라 칸만 1.6초(회전 0.9 + 확인 0.7)를 기다리고, 특수칸은 0을 돌려줘 흐름이 늘어지지 않게 했다.
순간이동(`teleportTo`)에도 같은 함수를 적용해 규칙을 한 곳에서 관리한다.

**대기 중 타일 클릭을 막은 이유.**
`isMoving = false`를 대기 전에 두면 학생이 그 사이 타일을 눌러 정보창을 띄워 지구본을 가릴 수 있다. `isMoving`을 대기가 끝날 때까지 유지하도록 순서를 바꿨다.

**나침반 제거.**
`compassSvg()` 함수와 호출부를 삭제했다. 북쪽이 항상 위이므로 정보량이 적고, 제목·지구본과 시각적으로 부딪혔다.

### 퀴즈 보상과 통행세 퀴즈 (2026-08-21)

**왜 통행세에 퀴즈를 붙였나.**
기존에는 남의 땅에 도착하면 돈만 빠져나갔다. 학생 입장에서 가장 손해가 큰 순간인데 배우는 게 없었다. 이제 그 나라 문제를 먼저 풀게 하고, 맞히면 정가·틀리면 1.2배를 낸다. **벌이 아니라 할인**으로 느껴지도록 정답을 기준값으로 잡았다. 1.5배나 2배는 한 번에 판이 뒤집혀 수업이 무거워진다.

**금액 상수.**
`landQuizReward = 5000`, `WRONG_TOLL_RATE = 1.2`. 수당 ₩5,000은 시작 자금 ₩350,000의 1.4%다. 땅값에 영향을 줄 만큼 크지 않으면서 "맞히면 받는다"는 감각은 남는다.

**`afterQuizAction` 을 둔 이유.**
퀴즈 확인 버튼(`claim-special`)은 원래 `endTurn()`만 불렀다. 통행세 퀴즈는 확인 뒤에 정산이 이어져야 해서, 다음 동작을 담는 변수 하나를 두고 핸들러가 있으면 그걸 실행하도록 했다. 콜백을 여러 개 만들지 않고 한 곳에서 분기한다.

**AI 경로는 `handleAIQuiz(..., onDone)`.**
AI 퀴즈 함수가 원래 수당 지급과 구매 판단을 스스로 했다. 통행세 상황에서는 그 두 가지가 일어나면 안 되므로, `onDone`이 넘어오면 내장 처리를 건너뛰고 결과만 넘긴다.

**구매 버튼 가로 배치.**
세로 배치라 1280×720에서 모달이 154px 넘쳐 스크롤이 생겼다. `#purchase-actions`를 2열 그리드로 바꾸고, `@media (max-height: 820px)`에서 카드 여백·글자를 줄였다. 넘침 0px.

**무작위 출제는 이미 되어 있었다.**
선생님이 "같은 순서로 나온다"고 하셨지만 `pickQuiz()`는 처음부터 남은 문제 중 무작위로 골랐다. 12게임을 새로 돌려 첫 문제 번호를 재니 5,1,5,3,2,3,5,0,4,5,5,2로 전부 달랐다. **브라우저 캐시로 옛 script.js가 남아 있던 것**이 원인일 가능성이 높다. `index.html`의 `?v=` 값을 올려 두었다.

**검증 스크립트가 두 번째로 거짓 경보를 냈다.**
회귀 검증에서 "1인 AI 모드 퀴즈 모달 교착"이 떴다. 실제로는 AI가 문제를 읽는 3.8초 + 한 번 더 굴리기로 퀴즈가 연달아 뜨는 구간을, 검증 스크립트가 '클릭할 게 없으니 멈춤'으로 세고 있었다.
`autoPlay`의 교착 판정을 **화면 내용이 전혀 변하지 않을 때만 세도록**(질문·결과 문구까지 서명에 포함, 20회 연속 = 14초) 고쳤다. 고친 뒤 5회 재실행에서 전부 정상 완주했다.
교훈은 지난번과 같다. **자동 검증이 실패를 보고하면 게임이 아니라 검증 쪽을 먼저 의심할 것.** 연출용 대기 시간이 늘어날 때마다 판정 기준을 같이 올려야 한다.


## 2026-08-22 개선 작업 (무제한 판수 · 지구본 · 카드 이동 · 주사위 2개)

### 무제한 판수
`목표 라운드` 선택지에 **무제한(파산할 때까지)** 버튼을 넣었다(`data-rounds="0"`).
`targetMaxRounds === 0`을 '무제한'의 표시로 쓴다. 별도 플래그를 두지 않은 이유는 라운드 수를 읽는 곳이 여러 군데(종료 판정·상단 표시·순위 문구)라서, 값 하나로 판정하는 편이 빠뜨릴 위험이 적기 때문이다. 판정은 `isUnlimitedRounds()` 한 곳으로 모았다.
- 종료 조건에서 라운드 초과를 빼면, 남는 종료 조건은 `endByLastMan`(생존 1명) 하나뿐이다. 즉 파산이 나야 끝난다.
- 상단 표시는 `ROUND 07/∞`. 무제한일 때는 라운드 숫자를 목표치로 자르지 않는다(`roundLabel()`).

### 지구본 그래픽 깨짐 — 원인은 '뒤편 처리'
정사영 도법에서는 지구 뒤편의 점도 앞면과 똑같은 원 안으로 투영된다. 기존 코드는 뒤편 점을 그냥 **건너뛰고** 다음 보이는 점부터 다시 선을 그었다. 그 결과
1. 대륙 하나가 여러 조각으로 쪼개지고, 조각 사이를 가로지르는 직선이 생겼다.
2. 모든 고리를 `path` 하나에 몰아넣어 그렸기 때문에 조각들의 회전 방향이 엇갈리면 서로 상쇄되어 **땅이 통째로 사라지거나 바다가 땅으로 칠해졌다.**

고친 방식은 지도학에서 쓰는 것과 같다. **앞면에 보이는 구간만 잘라 내고, 잘린 자리끼리는 지구 가장자리(테두리 원)를 따라 이어 붙여 다시 닫힌 도형으로 만든다.**
- 앞뒤 경계점은 이분법(`limbBetween`)으로 찾는다. 그래야 선이 테두리에 정확히 붙는다.
- 가장자리를 **어느 쪽으로 도는지**는 고리가 감긴 방향으로 정한다(`ringSignedArea`, 구면 부호 면적). 이걸 하나로 고정하면 카스피해처럼 반대로 감긴 고리(땅에 뚫린 호수)에서 원을 한 바퀴 돌아 화면 전체가 땅이 된다. 실제로 이 버그를 겪었다.
- 감긴 방향이 음수인 고리는 호수로 보고 바다색으로 덮는다.
- 대륙은 고리마다 `path`를 따로 만든다. 한 `path`에 몰면 위의 2번이 다시 생긴다.

검증은 화면을 보는 대신 **경로를 숫자로 확인**했다. 지구본을 5도씩 돌리며 원판 안의 육지 비율을 재니 9.7%~53.6% 사이에서 변하고, 한 프레임(5도) 사이 최대 변화가 2.9%였다. 땅이 갑자기 사라지거나 화면이 통째로 칠해지면 이 값이 크게 튄다. 고치기 전에는 같은 측정에서 80%가 튀었다.
성능도 같이 좋아졌다. 위경도를 3차원 벡터로 미리 바꿔 두어 매 프레임 삼각함수를 다시 계산하지 않는다. 한 프레임 0.59ms.

### 카드로 이동할 때 출발지 월급
'세계 어디든 이동' 카드는 순간이동이라 걸어갈 때 주던 월급을 건너뛰었다. 말은 언제나 앞으로만 가므로, **목적지 번호가 지금 칸보다 뒤면 판을 한 바퀴 돌아 출발지를 지나는 것**이다(`passesStart`). 이때 걸어갈 때와 똑같이 ₩70,000을 준다.
나라 선택 목록에도 `· 출발지 경유 월급 +₩70,000`을 붙였다. 아이들이 고르기 전에 이득을 볼 수 있어야 선택에 의미가 생긴다.

### 주사위 두 개
주사위를 두 개 굴려 **눈의 합(2~12칸)**만큼 움직인다. 회전 상태를 주사위마다 따로 들고 있어야 해서(`dieSpins` 배열) 전역 변수 `dieSpinX/Y`를 배열로 바꿨다.
같은 눈이 나왔을 때 한 번 더 굴리는 '더블' 규칙은 **넣지 않았다.** 요청은 주사위 개수까지였고, 더블은 턴 구조와 파산 속도까지 바꾸기 때문에 따로 정할 문제다.
주사위가 두 개가 되면서 한 줄에 들어가야 할 폭이 늘어, `.die-scene` 크기를 `clamp(50px, 6.8vh, 92px)` → `clamp(40px, 5.6vh, 72px)`로 줄였다. 1280×800에서 주사위 줄 422px / 가운데 공간 484px.

### 캐시 주의
`index.html`의 `?v=` 값을 `20260822_01`로 올렸다. 이걸 안 올리면 학생 크롬북에 옛 파일이 남아 고친 내용이 반영되지 않는다. **파일을 고칠 때마다 같이 올릴 것.**

### 더블 규칙 (같은 눈이면 한 번 더)
'한 번 더 굴리기'를 나타내던 `extraRollPending`(참/거짓)을 **큐(`extraRollQueue`)로 바꿨다.**
더블과 '한 번 더 굴리기' 카드가 한 차례에 겹칠 수 있는데, 참/거짓 하나로는 두 기회가 한 개로 뭉개진다. 큐에 사유('double' / 'card')를 쌓으면 겹쳐도 각각 한 번씩 주고, 토스트 문구도 사유에 맞게 나온다.
**파산 처리와의 순서가 함정이었다.** 통행세를 내다 파산하면 `bankruptPlayer()`가 `endTurn()`을 부른다. 이때 남아 있던 기회를 그대로 쓰면 이미 탈락한 사람이 주사위를 굴린다. 그래서 `endTurn()`에서 현재 사람이 파산 상태면 기회를 주지 않고 큐를 비운다.
더블 3연속 벌칙(부루마불의 감옥 규칙)은 넣지 않았다. 규칙이 하나 더 늘면 6학년 수업에서 설명 비용이 커진다. 대신 더블이 이어지는 동안은 계속 굴리게 두었다 — 4연속 확률이 1/1296이라 실제 수업에서 문제가 될 정도는 아니다.

### 건물도 문제를 맞혀야 지을 수 있게
빈 땅 매입은 원래 퀴즈를 통과해야 했는데, 내 땅 증축은 **돈만 있으면 그냥** 지을 수 있었다. 학습이 목적인 게임에서 통행세를 가장 크게 올리는 행동이 문제 없이 이뤄지는 게 이상해서 매입과 같은 구조로 맞췄다.

**문제를 낼 가치가 없는 경우는 먼저 걸러 낸다.**
- 이미 건물 3단계(최대) → 문제 없이 통과
- 지을 돈도 무료 증축권도 없음 → 문제 없이 통과
문제를 풀게 해 놓고 "그런데 돈이 없네요"라고 하면 아이 입장에서 허탈하다. 판단을 `offerBuild()` 앞쪽으로 모았다.

**함수를 둘로 나눴다.** `offerBuild()`는 문제까지, `showBuildChoice()`는 맞힌 뒤 실제로 지을지 고르는 단계다. 사람은 퀴즈 모달의 `확인` 버튼(`afterQuizAction`)을 눌러 증축 모달로 넘어간다. 모달 두 개가 겹치지 않게 하려면 이 방식이 필요했다 — 퀴즈 모달을 닫고 나서 증축 모달을 열어야 한다.

**정답 수당 ₩5,000은 빈 땅 매입 퀴즈와 똑같이 준다.** 같은 '나라 칸 문제'인데 상황에 따라 주고 안 주면 아이들이 규칙을 외우기 어렵다.

**무료 증축권은 비용만 면제한다.** 문제는 그대로 풀어야 한다. 카드로 학습을 건너뛸 수 있으면 규칙에 구멍이 생긴다.

**AI도 같은 규칙이다.** `handleAIQuiz()`에 `kind` 인자를 더해 '건축 문제' 문구를 쓰게 했다. 기존 호출부(통행세)는 `kind`를 넘기지 않으므로 문구가 그대로 유지된다.

**튜토리얼(`tutorial.html`)도 같이 고쳤다.** 오늘 바뀐 규칙이 학생용 안내와 어긋나면 안 되므로 주사위 2개·더블, 증축 문제, 무료 증축권, 출발지 경유 월급, 무제한 모드 다섯 군데를 수정했다.

### 통행세 안내 글자가 작았던 진짜 이유
"맞히면 얼마, 틀리면 얼마"가 작게 보인 건 폰트 값이 작아서가 아니었다. `.setup-card p { font-size: 14px }`(선택자 점수 0,1,1)가 `.quiz-result`(0,1,0)를 **덮고 있었다.** `@media (max-height: 820px)` 안의 `.quiz-result`도 마찬가지로 졌다. 즉 `clamp(18px, 2.2vw, 24px)`라고 적어 두고도 화면에는 계속 14px로 나왔다.
선택자를 `.quiz-card .quiz-result`(0,2,0)로 한 단계 올려 되찾았다. **이 파일에서 `.setup-card` 안쪽 요소의 글자 크기를 바꿀 때는 선택자 점수를 먼저 확인할 것.**
금액은 `.quiz-amount`로 따로 감싸 본문의 1.5배(1280×720에서 29px, 1920×1080에서 36px)로 키우고, 맞혔을 때 금액은 초록·틀렸을 때는 빨강으로 칠했다. 액수 차이가 색으로도 보여야 아이들이 "틀리면 손해"를 즉시 안다.
같은 줄을 쓰는 빈 땅 매입·건축·기후/지형 문제 안내도 14px → 24px로 함께 커졌다.

## 2026-08-22 경제 개편 (재해·세금 / 통행세 인상 / 단계별 건물값)

### 왜 파산이 안 났는가 — 숫자로 확인
게임을 200라운드까지 300판 돌려 보는 계산 스크립트를 만들어 재 봤다(실제 `spaces` 데이터와 상수를 그대로 읽어 씀).
**고치기 전: 첫 파산이 중앙값 96라운드, 16%는 200라운드까지 아무도 파산하지 않았다. 30라운드 안에 파산이 나올 확률은 0%.**

원인은 통행세가 낮아서가 아니었다. **돈이 사라지는 곳이 한 군데도 없었다.**
- 통행세는 플레이어끼리 주고받을 뿐 판 전체의 돈은 그대로다.
- 땅·건물은 언제든 **원가 100%로 되팔 수 있어서** 자산이 줄지 않는다.
- 반면 월급은 계속 들어온다. 게다가 주사위를 두 개로 바꾸면서 한 바퀴가 9턴 → 4.6턴으로 줄어 **월급 수입이 사실상 두 배가 됐다.**
턴당 수입을 재 보면 대략 ₩28,000(월급 15,300 + 퀴즈·특수칸 6,000 + 완주 보너스 등 ×1.2 더블 보정)이다. 이만큼을 계속 받는 한 파산은 구조적으로 어렵다.

### 넣은 것
1. **재해·세금(`DISASTERS`)** — 나라 칸에 도착하면 25% 확률. 돈이 **은행으로 사라진다.** 8종.
   칸의 태그에 어울리는 재해가 먼저 뽑히게 했다(일본·아이슬란드=지진/화산, 태국·필리핀=홍수/태풍, 사우디·이집트=가뭄, 그 밖=토지세·재산세·환경 부담금). 안내창에 교과서 설명을 한 줄씩 붙여 학습 요소를 살렸다.
   금액은 가진 것에 비례하는 항(땅 수·건물 수·땅값 합·현금)과 정액을 섞고 천 원 단위로 반올림한다.
   **재해가 뜨면 그 차례는 거기서 끝난다.** 퀴즈까지 이어 붙이면 한 턴이 두 배로 길어져서다.
2. **통행세 인상** — `[0.35, 0.7, 1.2, 1.9]` → `[0.4, 1.5, 2.8, 4.5]`. ₩70,000 땅 기준 ₩24,500/49,000/84,000/133,000 → **₩28,000/105,000/196,000/315,000.** 빈 땅은 거의 그대로 두고 건물 올린 땅만 무섭게 했다.
3. **단계별 건물값(`BUILD_RATES`)** — 0.5 고정 → `[0.5, 0.9, 1.5]`. ₩70,000 땅이면 🏠 집 ₩35,000 → 🏘️ 마을 ₩63,000 → 🏰 랜드마크 ₩105,000. 건물마다 이름을 붙여 단계가 오른 게 보이게 했다.
4. **급매 회수율 60%(`SELL_REFUND_RATE`)** — 요청에는 없었지만 넣었다. 100% 환급이면 아무리 큰 통행세도 땅을 팔아 막고 자산이 그대로라, 파산이 영영 안 난다. 자산 순위는 장부가(`bookValueOf`), 급하게 팔 때만 60%(`sellValueOf`)로 갈라 두었다.

### 고친 뒤 숫자
**첫 파산 중앙값 96 → 40라운드, 30라운드 안에 파산 0% → 17%, 무제한 게임이 끝나는 시점 200+ → 89라운드.**
다만 **10~15라운드짜리 수업 게임에서는 여전히 파산이 거의 안 난다(2% 미만).** 파산은 결국 '건물 올린 남의 땅을 밟는 사고'에서 나오는데, 4인 기준으로 건물 2~3단계가 서기까지 20라운드쯤 걸리기 때문이다. 시작 자금과 월급을 절반으로 낮춰도 15라운드 안 파산은 4% 수준이었다.
→ **수업에서 파산을 보려면 무제한 모드로 25~30라운드 이상 돌려야 한다.** 시작 자금(₩350,000)·월급(₩70,000)을 낮추는 건 선생님 판단이 필요해 손대지 않았다.

### 은행 납부 경로
`showSellModal`·`handleAIShortfall`의 `ownerIndex`에 `null`을 넘기면 '은행에 내는 돈'이 된다. 모달 제목·라벨·버튼 문구가 '통행세' 대신 재해 이름으로 바뀐다. 못 내면 파산이고, 이때 땅은 채권자가 없으므로 다시 빈 땅이 된다(`bankruptPlayer(idx, null)`).

### 세금 이벤트를 빼고 자연재해만 남긴 이유 (선생님 지적)
"재산세가 흥미를 떨어뜨린다, 연관성도 없다"는 지적을 받고 **토지세·재산세·환경 부담금 3종을 삭제**했다. 맞는 지적이다. 고지서는 어느 나라에서 받아도 똑같아서, 아이 입장에서는 그 나라를 배우는 것과 아무 상관이 없다.
대신 **자연재해를 5종 → 14종으로 늘려 28개국 전부가 자기 기후·지형에 맞는 재해를 갖게** 했다. 세금이 메우던 자리(서안해양성·평야·냉대침엽처럼 재해가 없던 칸)를 새 재해로 채운 것이다.
- 추가: 산불, 폭설과 한파, 산사태, 토네이도, 모래폭풍, 폭염, 대서양 폭풍, 메뚜기 떼, 산호 백화
- 칸별 배정을 전수 확인했다. 미국=토네이도, 케냐=메뚜기 떼, 호주=산호 백화, 스위스·네팔=산사태·폭설, 그리스=산불처럼 교과서에서 다루는 사례와 맞물린다.
- **`not` 조건을 둔 곳이 두 군데.** 태풍은 `열대`에 걸리지만 `우림`(인도네시아)은 제외한다 — 적도 부근은 태풍이 만들어지지 않는다. '대서양 폭풍'에서는 `해안`을 빼서 태평양의 필리핀에 대서양 폭풍이 뜨지 않게 했다. **자연 현상을 게임 규칙으로 쓸 때는 이런 예외를 반드시 확인해야 한다. 아이들이 배운 것과 어긋나면 게임이 오히려 방해가 된다.**
- 돈이 빠지는 양은 그대로다. 세금이 맡던 '자산에 비례하는 부담'을 지진(건물 수)·홍수/산불(땅 수)·산사태(건물 수)·태풍/폭염(현금 비율)이 나눠 가졌다. 칸 가중 평균 ₩44,500으로 이전(₩40,000대)과 비슷하고, 계산상 첫 파산은 오히려 40 → 38라운드로 조금 빨라졌다.

## 2026-08-22 문제 은행 감수 (교육과정·교과서 대조 + 선택지 길이 편향 제거)

### 무엇을 근거로 봤나
- `사회과 교육과정.pdf`(교육부 고시 제2022-33호) 316쪽에서 [6사09-01·02], [6사10-01·02], 그리고 '자연환경과 인간생활' 영역의 핵심 아이디어·내용 요소를 뽑아 대조했다.
- `교과서.pdf` 44쪽(6-2 1단원 '세계의 자연환경')의 본문·사례를 전부 텍스트로 뽑아 대조했다.
- `6-2_사회_1단원_1주제_1회_문제.hwp`(20문항 단원평가)는 **문항 형식의 기준**으로 삼았다. 이 시험지의 선택지는 길이가 서로 비슷하다(예: ① 산지 ② 하천 ③ 기온 ④ 해안 ⑤ 사막).

### 확인한 핵심 기준
- **기후는 열대·건조·온대·냉대·한대·고산 6가지로만 구분한다.** 교육과정에 "기후 지역을 지나치게 세분하여 가르치지 않도록 주의"라고 못 박혀 있고, 추가로 제시할 수 있는 이름은 열대 우림·열대 사바나·서안 해양성·지중해성뿐이다.
  → **'온대 계절풍 기후', '열대 계절풍(몬순) 기후'는 교육과정에 없는 이름이다.** 문제·해설에서 지웠고, 보드 칸 태그도 `온대계절풍→온대기후`, `열대계절풍→열대기후`, `몬순/하천→계절풍/하천`으로 바꿨다. 계절풍은 '기후 이름'이 아니라 '바람 이름'으로만 쓴다.
  (태그를 바꾸면 재해 배정 조건도 어긋나므로 `DISASTERS`의 `fits` 목록도 같이 고쳤다. 태그를 만질 때는 항상 재해 배정을 함께 확인할 것.)
- 한대 기후 지역 생활 모습은 **사냥·순록 유목·지하자원 개발·오로라 관광**이다. 영구 동토층 말뚝 기초는 교과서에 없어 문항을 교체했다.
- 자연재해·기후변화는 5~6학년 내용 요소에 **실제로 들어 있다.** 이번에 넣은 재해 이벤트는 교육과정 밖의 장치가 아니다.

### 선택지 길이 편향 — 측정하고 고쳤다
선생님 지적대로 정답만 길고 자세했다. 재 보니
**204문항 중 111개(54.4%)에서 정답이 가장 길었고, 정답은 오답보다 평균 3.8자 길었다.**
무작위라면 33%여야 하므로, 아이가 내용을 몰라도 **가장 긴 선택지를 찍으면 절반 이상 맞는** 상태였다.

고친 뒤: **정답 최장 57개(27.9%), 평균 길이차 0.28자, 정답이 오답보다 3자 이상 긴 문항 0개.**

원칙은 두 가지였다.
1. **정답에서 군더더기를 뺀다.** 괄호 설명(`논라(삿갓 모양 모자)`→`논라`), 이중 표기(`2기작·3기작`→`2기작`), 이유 나열(`습기와 열기, 해충과 홍수를 피하려고`→`습기와 열기, 홍수를 피하려고`).
2. **오답을 정답과 같은 문법 꼴로 맞춘다.** `땅속에서`(4자) 같은 성의 없는 오답을 `남서쪽 사막에서`(8자)처럼 정답과 같은 형태로 다시 썼다. 오답이 그럴듯해야 문제가 된다.
**해설은 줄이지 않았다.** 해설은 답을 고른 뒤에 나오므로 길수록 좋고, 실제 학습이 일어나는 곳이다.

### 문항 교체
- `영국#4` 산업 혁명 → **북해 연안 갯벌**(교과서에 나오는 세계적 갯벌 목록)
- `독일#3` 자동차 산업 → **하천 주변에 평야가 생기는 까닭**(교과서 Q&A 그대로)
  둘 다 '세계의 자연환경' 단원과 무관한 인문·역사 문항이었다.
- `기후#1` 영구 동토층 말뚝 기초 → **한대 기후 지역의 생활 모습**
- `베트남#5` '열대 계절풍 기후의 일 년' → **'건기와 우기가 번갈아 나타나는 열대 기후'**
- `기후#2`/`기후#12`의 기온 감률이 서로 달랐다(1,000m당 6.5℃ vs 100m당 0.6℃). 0.6℃/100m로 통일했다.

### 작업 방식 (다음 사람을 위해)
`questions.js`를 손으로 고치지 않았다. 스크립트로 JSON ⇄ JS 왕복 변환을 만들고(무손실 확인), 패치 파일을 적용한 뒤 길이 통계·선택지 중복·정답 번호를 자동 검증했다. 문항이 204개라 손으로 고치면 반드시 어긋난다.

## 2026-08-22 문제 수준 점검 (학교 단원평가 10회분과 대조)

폴더의 한글 파일 10개(1주제 1~5회, 2주제 1~5회, 총 150문항)를 전부 텍스트로 뽑아 우리 문제와 견줬다. **문항을 가져다 쓰지는 않았다.** 대조한 것은 수준과 출제 방식이다.

### 예상과 달랐던 것 — 발문 길이는 문제가 아니었다
우리 문제가 장황한 줄 알았는데 재 보니
- 학교 시험지 발문: 평균 **40.9자**, 40자 넘는 문항 48%
- 우리 문제 발문: 평균 **42.2자**, 40자 넘는 문항 57%
거의 같다. **발문은 손대지 않았다.** 짐작으로 고쳤으면 멀쩡한 걸 망칠 뻔했다.

### 진짜 문제는 오답이었다
학교 시험지의 오답은 **전부 교과서 안에서 가져온 진짜 내용**이다. 한대 기후를 묻는 문항의 오답이 카사바(열대)·커피(열대)·라마(고산)·사막 캠핑(건조)인 식이다. 그래서 다른 기후를 헷갈리는 아이를 정확히 걸러 낸다.
우리 문제에는 **'호주에 태양이 두 개여서', '모래가 얼음으로 변하기 때문에', '바다 밑에 자리하기 때문에'** 같은 오답이 16문항에 있었다. 아이가 내용을 하나도 몰라도 웃기는 걸 지우면 답이 남는다.
→ 16문항의 오답을 **다른 기후·지형 지역의 실제 모습**으로 바꿨다. 예를 들어 프랑스의 서안 해양성 기후 문항은 오답이 지중해성(여름 건조·겨울 강수)과 냉대(긴 겨울·많은 눈)의 실제 특징이 되었다. 이제 세 기후를 구별해야 풀 수 있다.

### 다른 과목 문항 4개를 단원 안으로
과학·역사 지식이었던 문항을 자연환경 단원 내용으로 바꿨다.
- 호주 유대류(과학) → **산호 백화 현상**(기후변화, 내용 요소에 있음)
- 캐나다 메이플 시럽(특산물) → **냉대 기후의 옷차림**(오답은 열대·건조의 실제 옷차림)
- 멕시코 마야·아스텍 문명(역사) → **멕시코의 자연재해**(지진·태풍)
- 브라질 삼바 카니발(문화) → **화전 농업**

### 남겨 둔 차이 — 이건 고칠 문제가 아니다
정답 낱말이 교과서·시험지에 없는 문항이 아직 82개다. 메콩강·안데스산맥·낙타·오로라·백야 같은 것들이다.
**교과서는 20여 곳만 다루지만 이 게임은 28개국을 돈다.** 교과서 안으로만 좁히면 같은 나라 문제가 계속 반복되어 보드게임이 성립하지 않는다. 대신 **묻는 수준**을 시험지와 맞췄다 — 낱말을 외웠는지가 아니라 '기후·지형이 생활에 어떤 영향을 주는지'를 묻는다.

또 하나, **우리는 3지선다이고 학교 시험지는 5지선다다.** 찍어서 맞을 확률이 33% 대 20%로 다르다. 보드 화면 크기 때문에 3개로 정했는데, 선생님이 원하시면 4개로 늘릴 수 있다(모달 세로 공간은 여유가 있다).

## 2026-08-22 최종 전수 검사 (더블 동작 + 204문항 재감수)

### 더블은 정상 작동한다
강제 더블과 자연 발생 더블을 모두 확인했다. 특히 **재해가 터져 차례가 일찍 끝나는 경우**를 일부러 만들어 봤다.
`플레이어 1 3+3=6 (더블)` → 인도네시아 착지 → 화산 폭발 복구비 납부 → **`플레이어 1 4+5=9`(한 번 더)** → `플레이어 2` 인계.
자연 발생한 `5+5=10 (더블)`도 똑같이 한 번 더 굴린 뒤 넘어갔다. 파산자에게 기회가 넘어가지 않는 것과, 더블+카드가 겹치면 두 번 다 주는 것도 확인했다.

### 204문항을 처음부터 다시 읽고 찾은 것
**발문에 정답이 그대로 들어 있던 문항 4개.** '해저에서 일어난 지진…' → 답 `지진해일`, '계단 모양의 논밭을…' → 답 `계단식 논밭`, '대형 농기계와 비행기를 활용하여…' → 답 `큰 농기계를 쓰는 농업`, '독일 최대의 공업 지역…' → 답 `루르 공업 지역`. 읽기만 해도 답이 보였다. 발문을 고쳐 단서를 없앴다.
기계 검사로 27건이 걸렸지만 대부분은 '○○ 기후를 무엇이라 하나요? → 온대 기후'처럼 **선택지가 모두 공유하는 낱말**이라 문제가 아니었다. 걸린 걸 다 고치는 게 아니라 하나씩 봐야 한다.

**교과서 밖 개념이 남아 있던 문항 10개.** 지난번에 옵션 길이만 다듬고 개념은 그대로 뒀던 것들이다.
- `기후#4` 건조 기후를 '증발량 > 강수량'으로 묻던 것 → **기온과 강수량이라는 기후 구분 기준**(교과서 핵심)
- `기후#12` 0.6℃/100m 수치 암기 → **적도 부근 고산에 사람이 모여 사는 까닭**
- `기후#13` 내륙의 기온 차(대륙성) → **위도가 같아도 기후가 달라지는 까닭**
- `기후#9` 편서풍 → **건조 기후 지역의 생활 모습**(오답은 냉대·열대의 실제 모습)
- `기후#3` 스콜 → **열대 기후의 옷차림**
- `지형#2` 칼데라호 → **산지 지형의 특징**
- `지형#10` 석회동굴, `지형#15` 석호, `지형#16` 상류·하류 자갈 → 각각 **하천 이용 / 해안 생활 / 관광 산업의 명암**
- `아이슬란드#0` 답이 '아이슬란드'였다. 아이슬란드 칸에서 나오는 문제라 답이 자명했다 → **빙하**
석회동굴·석호·상류하류는 4~5학년 우리 국토 내용이라 세계 단원과 결이 달랐다.

**표현 손질 11곳.** '교과서에 소개된'(게임 안에서 어색), '피오르(Fiord)'(영문 병기), '고온 건조한'(→덥고 건조한), '정체 전선'(→찬 공기와 따뜻한 공기가 만나), '고원 기후'(없는 명칭 →고원 지대), '세계 최대의 해안 지형'(산호초는 해안 지형이 아님), 사우디 해설에 남아 있던 증발량 정의 등.

### 자동 검사 항목 (다음에도 이대로 돌릴 것)
구조 오류 0 / 발문 중복 0 / 같은 풀 안 정답 중복 0 / 교육과정 밖 기후 이름 0 / 시험지와 동일한 발문 0 / 출제 500회 오류 0.
`서안 해양성 기후`가 금칙어 검사에 걸렸지만 **교육과정이 명시적으로 허용한 이름**이다(온대 기후는 서안 해양성·지중해성까지 제시 가능). 검사에 걸렸다고 무조건 고치면 안 된다.

## 2026-08-23 배포 전 3인 검증 + 최고 선임 통합 판정, 그리고 조치

학습 내용 / 게임 로직 / 디자인을 각각 다른 검토자에게 맡기고, 최고 선임이 교차 검증해 판정했다. 판정은 **조건부 배포 가능**이었고, 배포 차단 4건과 저비용 항목을 그 자리에서 고쳤다.

### 검토가 밝혀낸 가장 큰 것 — "잘 만든 학습 내용이 화면까지 도달하지 못했다"
게임 엔진과 문항은 튼튼했다(턴 데드락 0, 문항 정답 오류 0/816, 4해상도 넘침 0). **고장 난 곳은 정확히 가르치는 부분이었다.**

1. **CSS 특이도 싸움에서 해설이 지고 있었다.** `.setup-card p`(0-1-1, 14px)가 `.quiz-explanation`(0-1-0, clamp 16~20px)을 이겨, 1920 전자칠판에서 **발문 29px·보기 23px인데 해설만 14px**로 나왔다. 재해의 교과 연계 문장은 `.92em`이 14px에 걸려 **12.9px, 앱 전체 최소 크기**였다. 같은 원인으로 매각·게임종료 안내문도 14px.
   → `.setup-card p.quiz-explanation` 식으로 특이도를 맞췄다. **이 파일에서 `.setup-card` 안쪽 글자 크기를 건드릴 때는 반드시 특이도부터 확인할 것.** 이미 `.setup-card p.info-desc`가 같은 방식으로 해결돼 있었는데 나머지가 따라가지 않은 것이 원인이었다.
2. **틀렸을 때 정답을 알려 주지 않았다.** 오답 분기 5곳 전부 오답만 빨갛게 칠하고, 그중 땅 매입·건축 2곳은 1.8초 뒤 창이 닫혔다. 아이가 "그럼 답이 뭐예요?" 하는 순간 넘어간다. 퀴즈형 학습앱에서 가장 치명적인 결함인데, 코드만 봐서는 눈에 띄지 않았다.
   → `revealAnswer()`를 만들어 5곳 모두 정답을 표시하고, 자동 종료를 없애 '확인' 버튼을 기다리게 했다. 결과 문구도 "정답은 ○○ 입니다"로 바꿨다.

### 배포 차단이었던 나머지 둘
3. **무제한 모드에 멈출 방법이 없었다.** 최고 선임 재측정 결과 4인 결착까지 **중앙 158라운드 / 532턴**(부하 추정 92라운드보다 나빴다). `#restart-game`은 게임오버 모달 안에만 있어 게임 중에는 누를 수 없다. 종이 치면 새로고침 = 순위 소실.
   → 모드를 없애는 대신 **상단바에 '🏁 게임 끝내기' 버튼**을 만들었다. `checkGameOver('manual')`로 현재 자산 순위를 발표한다. 무제한뿐 아니라 **모든 모드에서 종이 칠 때 쓸 수 있는 안전장치**라 이쪽이 낫다. 되돌릴 수 없으므로 확인 창을 한 번 거친다.
4. **콩고 칸이 세 가지 다른 나라를 가리켰다.** 이름은 `콩고(공)`(콩고 공화국), 국기 코드는 `cd`(민주공화국), 좌표도 민주공화국, 해설은 "콩고 민주 공화국 와게니아족". 교과서 표기는 민주공화국.
   → `콩고(민)`으로 통일. **`questions.js`의 풀 키도 반드시 함께 바꿔야 한다** — `countryQuiz(spaceName)`이 칸 이름으로 조회하므로 한쪽만 바꾸면 그 칸에서 문제가 안 뜨고 턴이 조용히 넘어간다.

### 부하 보고를 액면 그대로 믿지 않은 것이 도움이 됐다
최고 선임이 6건을 직접 재확인해 **3건을 정정**했다.
- "종료 후 AI 카드·재해 타이머가 계속 돈다" → **사실이 아니었다.** `resolveLanding`에 가드가 있다. 가드가 빠진 곳은 `movePlayerStepByStep` 하나뿐이고, 실제 피해는 월급 ₩70,000이 더 지급돼 순위표와 카드 금액이 어긋나는 것뿐. [치명]에서 내렸다. (가드는 추가했다)
- "이름 `innerHTML` 주입은 보안 문제" → 로컬 전용·`maxLength=10`이라 화면 깨짐 수준. 등급을 내렸다.
- **"`--orange`를 `#c2410c`로 바꾸면 14건 전부 AA 통과" → 틀렸다.** 라운드·타이머 칩(`#f0ebe1`) 위에서 4.36:1로 여전히 미달이었다. 실제로 재 보니 그랬다. **색과 함께 `.status-pill strong`을 12px→16px로 키우고, 색도 `#b23c0a`로 한 단계 더 어둡게** 해야 다섯 배경 전부에서 4.5:1을 넘긴다(칩 4.99 / 종이 5.26 / 카드 5.83 / 상단바 4.72 / 금색행 5.57).
**교훈: 대비 수치는 "어느 배경 위에서"를 빼면 의미가 없다.**

### 함께 고친 저비용 항목
- 킬리만자로를 고산 기후 예시로 든 것(정상부 연중 영하) → **에티오피아 고원**(교과서·학교시험에 실제 등장)
- "물을 오래 견디고"(뜻이 안 통함) → "물을 마시지 않고도 오래 견디며"
- "아이슬란드를 덮고 있으며"(빙하 피복률 11%) → "곳곳을"
- 사우디·네팔이 아프리카 색이었다 → 아시아 색. **색이 대륙 단서로 읽히므로 [6사09-02]에 직접 역행했다.**
- `pulse-time`·`pulse-border` keyframes가 **참조만 있고 정의가 없어** 시간 경고 깜빡임이 조용히 작동하지 않았다 → 정의 추가
- 비활성 보기가 UA 기본값 때문에 대비 2.0:1이라 사실상 안 보였다 → `:disabled` 규칙 추가
- 미국#0 해설이 정답을 되풀이만 하던 유일한 문항(204개 중) → 이유를 설명하도록 교체
- 기후#6 오답 '사바나 기후' → 교육과정 원문 표기인 '열대 사바나 기후'

### 남긴 것 (수업 진행에는 지장 없음)
1280×720 4인에서 4번 카드가 31px 잘림, 매각·파산에 확인 절차 없음, 토스트가 주사위 버튼을 덮음, 정답/오답 색 명도비 1.03:1(✓/✕ 글리프 권장), 이탈리아에 에트나 화산 없음, '판'·'편서풍'이 교과서에 없는 말, 크레딧이 좌하단 타일을 덮음.

### 밸런스에 대해 확인된 사실
10라운드에서 **건물 2단계 도달 판당 0.03칸, 3단계 0건**. `BUILD_RATES[1..2]`와 `TOLL_RATES[2..3]`은 수업 시간 안에는 사실상 죽은 규칙이다. **교사에게는 '집'까지만 설명하라고 안내하는 편이 낫다.** 반대로 1위/4위 자산 격차는 10R에서 1.55배로 건강하다.
