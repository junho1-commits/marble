# 교실 속 세계일주 — 플레이 테스트 결과

테스트 방법: Chromium + Playwright로 실제 브라우저에서 자동 플레이.
2인 라운드제, 1인 AI 대전, 4인, 시간 제한제를 각각 진행하고 규칙별 표적 검증을 수행했습니다.
해상도는 1366×768(크롬북 표준), 1280×720, 900×700에서 확인했습니다.

정상 동작한 것 — 게임 진행 전반, 주사위/이동 애니메이션, 퀴즈 정답 판정, AI 자동 턴, 라운드·시간 종료, 순위 발표, 자바스크립트 런타임 오류 0건.

---

## 치명적 (수업 중 게임이 멈춤)

### 1. 파산 선언 후 게임이 완전히 정지합니다

`script.js` `bankruptBtn.onclick` (약 1002행)

통행세를 낼 현금이 없고 매각할 땅도 없어 "파산 선언" 버튼을 누르면, 자산만 넘어가고 `endTurn()`이 호출되지 않습니다.
`checkGameOver('bankrupt')`는 라운드 초과 또는 시간 종료일 때만 게임을 끝내므로 `'bankrupt'`는 아무 조건에도 걸리지 않고 `false`를 반환합니다.

결과 — 게임 종료 모달도 안 뜨고, 주사위 버튼은 비활성 상태로 남고, 다음 턴으로도 넘어가지 않습니다. 새로고침 외에는 방법이 없습니다.

재현 확인 — 통행세 ₩120,000 / 보유 현금 ₩1,000 상황에서 파산 클릭 후 주사위 버튼을 눌러도 반응 없음, `isGameFinished=false`, `currentPlayerIndex`는 그대로.

수정 방향 — `checkGameOver`에 파산 처리 분기를 추가하거나, 파산한 플레이어를 턴 순서에서 제외한 뒤 `endTurn()`을 호출해야 합니다.

---

## 심각 (교육 내용이 잘못 전달됨)

### 2. 나라 사진이 전부 한 칸씩 밀려 있습니다

`script.js` `landmarkPhotos` 배열 (551행)

배열 0번에 "한국" 사진이 들어 있는데 `spaces` 0번은 "출발지"입니다. 특수칸 4개(출발지·기후 퀴즈·지형 퀴즈·생태 쉼터)를 배열에 넣지 않아 전체가 한 칸씩 어긋났습니다.

실제 화면에서 확인한 결과입니다.

| 타일 | 실제로 표시되는 사진 |
|---|---|
| 한국 | 일본 사진 |
| 일본 | 베트남 사진 |
| 베트남 | 태국 사진 |
| 인도 | (빈값 → 기본 아이슬란드 사진) |
| 아이슬란드 | (빈값 → 기본 아이슬란드 사진) |
| 네팔 | (빈값 → 기본 아이슬란드 사진) |
| 뉴질랜드 | (배열 길이 31개뿐 → 기본 사진) |

기후·지형을 사진으로 배우는 수업인데 모든 나라 사진이 옆 나라 것으로 나옵니다. 배열 길이도 31개라 32칸에 하나 모자랍니다.

수정 방향 — 배열 대신 `spaces` 각 항목에 `photo` 속성을 직접 넣으면 다시 어긋날 일이 없습니다.

### 3. 출발지에 정확히 도착하면 월급을 두 번 받습니다

`script.js` `movePlayerStepByStep` 1301행 + `resolveLanding` 1129행

이동 중 0번 칸을 밟으면 월급 ₩70,000을 주고, 착지 처리에서 `special-start`라며 ₩70,000을 또 줍니다.

측정값 — 출발지 정확히 도착 시 **₩140,000**, 지나가기만 하면 ₩70,000.

수정 방향 — `resolveLanding`의 `special-start` 분기에서 지급을 빼면 됩니다(이동 중 이미 지급됨).

### 4. 안내문에 적힌 금액과 실제 지급액이 다릅니다

타일을 클릭하면 뜨는 설명 카드의 금액이 코드와 맞지 않습니다.

| 칸 | 안내문 | 실제 지급 |
|---|---|---|
| 출발지 | ₩50,000 | ₩70,000 (도착 시 ₩140,000) |
| 생태 쉼터 | ₩20,000 | ₩25,000 |
| 기후/지형 퀴즈 | ₩30,000 | ₩30,000 (일치) |

학생이 안내문을 읽고 계산하면 답이 안 맞습니다.

---

## 수업 진행에 불편한 점

### 5. 돈이 오가는데 화면에 아무 안내가 없습니다

퀴즈가 없는 상황은 전부 조용히 지나갑니다. 실제 측정한 금액 변화입니다.

- 출발지 착지 → +₩70,000, 모달 없음
- 생태 쉼터 착지 → +₩25,000, 모달 없음
- 남의 땅 통행세 → −₩70,000, 모달 없음
- 내 땅 증축 → −₩32,500, 모달 없음 (게다가 **선택권 없이 자동 증축**)
- 건물 3단계인 내 땅 재착지 → 변화 없음, 아무 표시 없음

사이드바 숫자만 바뀌므로 학생이 왜 돈이 줄었는지 모릅니다. `addActivityLog()`는 콘솔에만 찍히고 화면 패널이 제거된 상태입니다.

수정 방향 — 짧은 토스트 알림이나 기존 퀴즈 모달을 재활용한 안내 화면 추가. 증축은 "지을까요?" 확인을 받는 게 자연스럽습니다.

### 6. 라운드 표시가 목표치를 넘어갑니다

10라운드 목표에서 게임이 끝나는 순간 화면에 **ROUND 11/10**이 표시됩니다. `endTurn`에서 라운드를 올리고 화면을 갱신한 뒤에 종료 판정을 하기 때문입니다.

### 7. 시간 종료 시 열려 있던 퀴즈 모달이 정리되지 않습니다

퀴즈 푸는 도중 시간이 끝나면 게임 종료 모달과 퀴즈 모달이 동시에 열립니다. 두 모달의 `z-index`가 똑같이 50이라 DOM 순서로 겨우 가려지는 상태입니다. 또한 그 퀴즈의 결과는 순위에 반영되지 않습니다.

### 8. AI는 파산하지 않습니다

`handleAITollPayment` — AI는 통행세가 모자라면 가진 만큼만 내고 턴을 넘깁니다(부분 납부). 사람은 파산 처리되는데 AI는 현금 0원으로 계속 게임합니다. 규칙이 서로 다릅니다.
참고로 주석에는 "비싼 것부터 순차 매각"이라 적혀 있지만 실제로는 보드 순서(인덱스 오름차순)로 팝니다.

### 9. 이동 중에도 타일 정보창이 열립니다

말이 움직이는 도중 다른 칸을 클릭하면 정보창이 뜨고, 이동이 끝난 뒤에도 열린 채 남습니다. 게임 진행을 가리게 됩니다.

---

## 디자인 / 표시

### 10. 사진이 안 뜨면 검은 칸만 남습니다

타일 배경을 `images.unsplash.com` 외부 주소로 불러옵니다. 학교 망에서 차단되거나 인터넷이 느리면 어두운 남색(`rgb(45,69,84)`) 배경에 글자만 남습니다. 읽히기는 하지만 지리 사진으로 배우는 취지가 사라집니다.

수정 방향 — 사진을 내려받아 `images/` 폴더에 두고 상대 경로로 참조하면 오프라인에서도 동작합니다. 저작권 표기도 함께 정리하시는 게 좋습니다.

### 11. 태그 글자가 15개 칸에서 잘립니다

1366×768에서 측정한 잘림 목록입니다.

- 태그 잘림 (15칸) — 한국(온대계절풍), 베트남(열대계절풍), 프랑스·영국(서안해양성), 그리스(지중해해안), 호주(산호초해안), 캐나다(냉대/침엽수), 노르웨이(피오르해안), 페루(안데스고산), 브라질(이구아수/아마존), 모로코(사하라관문), 콩고(콩고강어업), 케냐(열대사바나), 스위스(알프스고산), 생태 쉼터(지구촌보호)
- 나라 이름 넘침 (2칸) — 인도네시아, 아이슬란드 (칸 폭 73px에 78px 글자)

기후 이름이 핵심 학습 요소인데 "온대계..."처럼 잘려서 안 보입니다.

### 12. 출발지 칸이 말에 가려집니다

4인 플레이 시 출발지에 말 4개가 모이면 그중 2개가 "출발지" 글자와 겹칩니다. 게임 시작 화면에서 모서리 칸 이름이 안 보입니다.

### 13. 좁은 화면에서 플레이어 현황이 아래로 밀립니다

900px 폭 이하에서는 보드 아래로 사이드바가 내려가 스크롤해야 보입니다. 1280×720, 1366×768에서는 정상입니다.

---

## 게임 밸런스 (참고)

- 주사위가 1개(1~6)라 한 바퀴(32칸)에 평균 약 9턴이 걸립니다. **5라운드를 고르면 반 바퀴밖에 못 돌고**, 10라운드가 딱 한 바퀴입니다. 출발지 월급을 받을 기회가 거의 없습니다.
- 통행세는 `땅값 × (건물수+1) × 0.5`입니다. 그리스(₩60,000) 기준 건물 3단계면 통행세 ₩120,000으로, 시작 자금 ₩350,000의 3분의 1이 한 번에 나갑니다.
- 특수칸 퀴즈가 기후 3문제, 지형 3문제뿐이라 금방 반복됩니다.
- 각 나라 퀴즈는 1개씩이고 땅이 팔리면 다시 안 나오므로, 한 게임에서 학생이 접하는 문제 수가 제한적입니다.

---

## 우선순위 제안

1. 파산 정지 (1번) — 수업 중단으로 이어짐
2. 사진 밀림 (2번) — 교육 내용 오류
3. 월급 이중 지급 + 안내문 금액 (3·4번) — 규칙 신뢰도
4. 돈 이동 안내 부재 (5번) — 학습 효과
5. 태그 잘림 (11번) — 기후 용어 가독성
